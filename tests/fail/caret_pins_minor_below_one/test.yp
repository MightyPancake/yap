// Below 1.0 the caret pins the minor, so ^0.9.0 rejects 0.10.0 even though
// 0.10.0 is the newer version.
module caretminor {
    version: "0.1.0",
    deps: ["vertest@^0.9.0"]
}

import vertest

i32 fn main(){
    ret 0;
}
