i32 fn add(i32 a, i32 b){
	1+2
}

i32 fn some_func_using_funcs(
	(fn) a,
	(fn) a2,
	(i32 fn) b,
	(i32@ fn i32@) c,
	i32 num,
	f32 float
){
	d := 123
	// f2 := float + 1
	res := add(1, 2)
	//res(1)
	//add(1,2,3)
	//add(float, 1)
}
