//This test checks file imports (at least for now as modules are not a thing yet)

import "imports/foo_bar.yp"
import "imports/foo.yp"
import "imports/bar.yp"

//import math

i32 fn main(){
    //foo();
    ret 0;
}