// strlen stops at the first null byte, giving "hello\x00world" .len = 5 instead of 11.
// Fixed by propagating decoded length (value_len -> text_len) past strlen.

i32 fn main() {
    byte[] s = "hello\x00world";
    if (s.len == 11) ret 0;
    ret 1;
}
