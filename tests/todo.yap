none fn main(i32 argc, byte@@ argv){
    //Literals, var declaration and type casting
    a := 12.(i64)
    b := 6.(f32)
    
    ptr := a@
    //ptr = 3@

    if (a) b = 1

    if (a+a) b=2
    else b=3

    a++
    b--
}
